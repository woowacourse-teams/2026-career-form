#!/bin/sh
set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"
. "$ROOT/.githooks/python-runtime.sh"
PYTHON=$(select_python "$ROOT")
"$PYTHON" -m unittest discover -s harness/tests
git diff --cached --check
