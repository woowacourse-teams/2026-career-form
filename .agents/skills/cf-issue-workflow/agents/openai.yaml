interface:
  display_name: "Issue Workflow"
  short_description: "ready Issue를 계약 브랜치에서 Draft PR까지 연결"
  default_prompt: "Use $cf-issue-workflow to implement ready GitHub Issue #123 on its approved CF or hotfix branch through one verified Draft PR."
