# HANDOFF 사용 중단 안내

이 저장소는 공용 `HANDOFF.md`에 작업 상태를 기록하지 않는다. 여러 사람이 같은 파일을 갱신하면서 생기는 충돌과 오래된 상태를 막기 위한 결정이다.

새 작업과 재개 작업은 GitHub Issue, 연결 PR, `git rev-parse --git-path cf-workflow/checkpoint.json`과 `cf-workflow/plan.md`, 현재 브랜치 순서로 확인한다. 이 파일에는 진행 상태, 완료 항목, 검증 결과를 추가하지 않는다.
