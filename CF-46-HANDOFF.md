# CF-46 세션 인수인계

## 실행 상태

- 브랜치: `CF-46`. 변경사항은 커밋·푸시하지 않았음.
- 백엔드: `http://127.0.0.1:8080` 실행 중. MongoDB `127.0.0.1:27018` 연결 확인함.
- 확장프로그램 production 설정: `VITE_API_BASE_URL=http://127.0.0.1:8080`.

## 문제 → 해결 기록

### 직무별 SK 폼 차이로 공통 입력까지 차단됨

- 문제: 직무별로 일부 DOM이 달라지면 전체 지원서 구조 분석을 실패 처리했음.
- 해결: SK 정책의 필수 공통 구조와 선택 구조를 분리했음. 공통 구조만 검증되면 공통 필드는 기입하고, 없는 선택 구조는 건너뜀. 검증되지 않은 DOM은 계속 입력 불가로 처리함.

### 조건부 항목이 첫 실행에 열리지 않아 재실행이 필요함

- 문제: 보훈·병역·장애, 복수·부전공처럼 선택 뒤 생성되는 DOM을 너무 일찍 분석했음.
- 해결: 라디오와 select를 준비 후보로 수집했고, 정책이 정확한 선택값과 기대 DOM 이름을 반환하도록 확장했음. `MutationObserver`로 필드 표시를 확인한 뒤 DOM을 다시 수집·분석함. 프로필이 `없음`일 때 `있음`을 누르던 버그도 막음.

### 중간 단계 UI가 결과 확인을 방해함

- 문제: 자동 기입 모달에 `1/3`, `2/3`, `3/3` 진행 단계가 남아 있었음.
- 해결: 중간 단계를 제거하고 성공 수와 직접 확인 필요 항목만 결과 화면에 표시하도록 정리했음.

### 프로필 정보와 backend 계약이 끊겨 자동 기입 후보가 누락됨

- 문제: 보조 이메일, 비상연락처, 거주 국가, 장애등록번호, 학업과정·합격일자, 재학 상태, 학교 소재지, 총 이수학점이 프로필·backend·회사 정책에서 일관되게 연결되지 않았음.
- 해결: 프로필 입력/저장 키와 backend canonical field를 추가했음. 학위구분을 `전문학사`/`학사` 드롭다운으로 바꾸고, 검정고시 선택 시 합격일자를 표시했음. 확인된 SK DOM만 정책에 매핑했음. 최상위 대학 행을 못 찾을 때 첫 항목으로 렌더링하는 보정도 추가함.

### 자격·어학·학력의 일부 필드가 빠짐

- 문제: 자격증명·발행처, 어학 언어·시험종류·점수, 재학 상태·학교 소재지·기준평점·총 이수학점·전공 관련 필드가 정책 또는 반복 행 연결에서 빠졌음.
- 해결: 확인된 SK DOM에 직접 정책 매핑을 보강했음. `LOOKUP`도 `DIRECT`와 같은 반복 행 규칙으로 해석하게 바꾸고, 동적으로 생긴 단일 대학 행은 첫 프로필 대학 행과 연결함.

### 학사가 전문대학(전문학사)로 기입됨

- 문제: 새 대학 행의 기본값이 기존 값 충돌로 판정돼 `학사` 선택이 실행되지 않았고, 옵션명 변환도 프론트에 있었음.
- 해결: Mongo SK 정책 `LOOKUP`으로 `전문학사 → 전문대학(전문학사)`, `학사 → 대학(학사)`를 반환하게 했음. 프론트는 정책의 정확한 옵션명만 선택함. 이번 실행에서 새 행의 기본값만 빈 값처럼 처리하고 사용자 기존 입력 행의 충돌 보호는 유지함. SK 후속 이벤트가 덮어쓰면 다음 이벤트 루프에서 승인된 select를 한 번 재적용함.

### 복수전공명·부전공명·어학 시험종류가 여전히 한 번에 안정적으로 안 들어감

- 문제: 조건부 선택 뒤 전공명 DOM이 생성되고, SK 후속 이벤트가 일부 텍스트 값을 비우거나 수집 시점에 필드가 빠짐.
- 현재 적용: 조건부 필드 표시를 기다린 뒤 스냅샷을 수집하고, 성공한 select와 텍스트를 다음 이벤트 루프에서 한 번 재작성함. 대학 행 추가 뒤 조건부 전공 선택용 준비 분석도 추가함.
- 남음: 실페이지에서 부전공명·어학 시험종류가 여전히 불안정함. 다음 세션에서 각 필드의 `분석 응답 → 승인 write plan → 즉시 DOM value → 다음 이벤트 루프 뒤 DOM value`를 한 실행에서 비교한 뒤 실제 원인만 수정해야 함.

### 장애정보가 자동 기입되지 않음

- 문제: SK의 `prsDisabledType`은 중증/경증이고 `prsDisabledTypeDtl`은 장애 유형인데, 기존 정책이 세부구분 하나만 연결했음. 장애등록번호의 실제 SK DOM은 아직 확인되지 않았음.
- 현재 적용: `prsDisabledType ← disabilityGrade`, `prsDisabledTypeDtl ← disabilityType`으로 정책을 분리했고, 장애 여부 `대상` 선택 뒤 두 필드를 기다리게 변경했음. 시더 테스트를 통과시키고 로컬 Mongo에 정책을 다시 시드했음.
- 남음: 실페이지에서 장애정보가 여전히 안 들어감. 다음 세션에서 최신 확장프로그램을 로드한 뒤 `/api/v1/fields/analyze` 결과가 두 DOM에 대해 올바른 `MATCH/profileFieldKey`를 반환하는지 확인해야 함. 분석 결과가 맞으면 프론트 write 문제, 결과가 틀리면 Mongo 정책/백엔드 문제임.

### 로컬 백엔드 재기동

- 문제: 기본 Mongo 포트 `27017`로 띄우면 로컬 DB가 없어 정책 시드가 대기했음.
- 해결: 실제 로컬 Mongo 포트 `27018`과 `local` 프로필로 재기동했고 `/actuator/health` HTTP 200을 확인함.

## 다음 세션 순서

1. 확장프로그램이 현재 `frontend/dist` 빌드인지 확인함.
2. 장애정보, 부전공명, 어학 시험종류를 한 번의 자동 기입으로 재현함.
3. 실패 필드를 분석 응답·write plan·즉시 DOM·후속 이벤트 뒤 DOM으로 분리해 원인을 확정함.
4. 확인된 계층만 수정하고 관련 테스트, 프론트 전체 테스트·typecheck·build, backend Gradle 테스트와 하네스 검증을 실행함.
5. 사용자 실페이지 검증 뒤에만 커밋·푸시 여부를 결정함.
