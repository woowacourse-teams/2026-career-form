# CF 스킬 라우팅 평가 기록

## 2026-08-13 Issue #14

### 입력

- 기획, ready Issue 구현, 고위험 결정 검토, 전체 생명주기와 머지 후 정리 요청을 비식별 문장으로 구성했다.
- 각 대상 스킬에 포함 사례와 배제 사례를 한 쌍으로 두었다.
- 팀원 환경에서 관찰된 비접두사 `issue-workflow`와 `superpowers` 중복 노출을 재현 조건으로 기록했다.

### 자동 결과

- `harness/scripts/validate-skill-routing-evals.py`: 통과
- 기대 스킬은 모두 실제 `cf-*` 폴더와 연결됐다.
- 포함 및 배제 쌍과 판단 근거가 모두 존재했다.

### 판단 범위

이 검증은 eval 구조, 실제 스킬 이름과 라우팅 기대값을 증명한다. Codex 런타임이 자연어 요청에서 해당 스킬을 선택했는지는 증명하지 않는다. 이름 변경 뒤 새 세션과 팀원 Windows 환경의 실제 선택 결과는 PR 수동 검증에 기록한다.

정적 HTML review 페이지는 만들지 않는다.
